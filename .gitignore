# Secrets / env
.env
.env.*
!.env.example

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json.bak

# Logs
logs/
*.log

# Build / cache
dist/
build/
.cache/
coverage/
assets/cache/
# Derived casino spin media (scripts/build-casino-spin-assets.js) — lives in R2,
# regenerated on demand; runtime falls back to on-the-fly render if absent.
assets/generated/

# Generated skin-alignment artifacts (scripts/alignSkins.js, scripts/skinPreview.js)
tmp/

# Recompression staging output (scripts/recompress-assets.js) — owner reviews
# then uploads to R2; never committed.
assets/_recompressed/

# Local documentation — not pushed
docs/
AGENTS.md
AGENT.md

# SQL scratch/backup dumps — deployment schema (and v4 rules schema) stay tracked
*.sql
!scripts/production-consolidated-schema.sql
!scripts/credd_schema_v4.sql
!scripts/canvas-cache-schema.sql
!scripts/avatar-system-schema.sql
!scripts/avatar-dev-owner-grants.sql
!scripts/deity-passive-description-update.sql
!scripts/migrations/*.sql

# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
